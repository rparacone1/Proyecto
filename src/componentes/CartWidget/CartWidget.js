import cart from ".assets/cart.svg";
import React from "react";

const CartWidget = () => {
  return (
    <div>
      <img src={cart} alt="cart-wdiget"></img>
    </div>
  );
};

export default CartWidget;
